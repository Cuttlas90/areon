
var baseUrl = 'https://areon-api.cuttlas.app';
var isAdmin = false;

function numberWithCommas(x) {
	//return x.toString().replace(/\B(?<!\.\d*)(?=(\d{3})+(?!\d))/g, ",");
	return x.toLocaleString();
}
